# Claude Code

Read and follow [AGENTS.md](AGENTS.md) before inspecting or changing this
project. It is the authoritative safety, architecture, validation, and release
contract for every coding agent.

For implementation procedures, change maps, scratch-graph testing, schema
evolution, UI verification, and release steps, also read
[docs/maintaining-and-extending.md](docs/maintaining-and-extending.md).

When these files conflict with a direct user instruction, follow the user while
preserving the immutable-capture, official-Logseq-API, privacy, backup, and
graph-identity safety boundaries unless the user explicitly changes those
product guarantees.
